{
  "name": "@livestreak/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./generated/contracts.ts"
  },
  "files": [
    "generated/",
    "foundry.toml",
    "src/"
  ],
  "scripts": {
    "clean": "rm -rf out cache generated/contracts.ts",
    "build": "forge build && FOUNDRY_PROFILE=aa forge build && wagmi generate",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "forge fmt --check src test",
    "test": "forge test"
  },
  "dependencies": {
    "viem": "^2.23.0"
  },
  "devDependencies": {
    "@wagmi/cli": "^2.10.0",
    "tsx": "^4.19.0",
    "typescript": "6.0.3"
  }
}
